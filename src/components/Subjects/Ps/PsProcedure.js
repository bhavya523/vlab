import React from 'react'

function PsProcedure() {
  return (
    <div>
        <h1>Numerical Representation</h1>
        <div>For each problem you have to write a program in C or C++. Each question is evaluted on test cases. An answer is correct only when all the test cases are cleared.</div>
    </div>
  )
}

export default PsProcedure